"""
Problem Statement:
Crie uma função validar_idade(idade) que LANCE um ValueError (usando raise) quando a idade for inválida:
- idade negativa → mensagem "a idade não pode ser negativa"
- idade maior que 130 → mensagem "a idade não pode passar de 130"
Se a idade for válida, a função retorna a própria idade.
No programa principal, chame a função dentro de um try/except e mostre a mensagem do erro.

Input:
idade -> inteiro

Output:
Se for válida:
"Idade registrada: {idade}"
Se for inválida:
"Idade inválida: {mensagem_do_erro}"

Examples:
Input:
25

Output:
Idade registrada: 25

Input:
-3

Output:
Idade inválida: a idade não pode ser negativa
"""

# Escreva sua solução abaixo
