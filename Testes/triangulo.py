"""
Problem Statement:
Reescreva o exercício "Formula_triangulos" (pasta Estruturas Condicionais) para que ele possa ser testado:
1. Crie a função classificar_triangulo(lado_1, lado_2, lado_3) que RETORNA uma das strings:
   "não é triângulo", "equilátero", "isósceles" ou "escaleno".
2. Coloque a leitura dos lados e o print dentro de um bloco if __name__ == "__main__":
3. Crie o arquivo test_triangulo.py nesta pasta, importe a função (from triangulo import classificar_triangulo)
   e escreva um teste para cada um dos 4 resultados possíveis.
4. Rode pytest e confirme que tudo passa.

Input (quando o arquivo é executado diretamente):
lado_1 -> inteiro
lado_2 -> inteiro
lado_3 -> inteiro

Output:
"Classificação: {resultado}"

Examples:
Input:
3
4
5

Output:
Classificação: escaleno
"""

# Escreva sua solução abaixo
